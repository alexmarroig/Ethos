{
  "name": "ethos-patient",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m http.server 5173 --directory .",
    "build": "echo \"static app: no build step\"",
    "preview": "python -m http.server 5173 --directory ."
  }
}
